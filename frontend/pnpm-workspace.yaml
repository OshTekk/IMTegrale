packages:
  - "."

allowBuilds:
  esbuild: true
  msw: false

overrides:
  js-yaml: 4.3.0

auditConfig:
  # This SPA uses BrowserRouter only; the advisory applies exclusively to unstable RSC APIs.
  ignoreGhsas:
    - GHSA-qwww-vcr4-c8h2
